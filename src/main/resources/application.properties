spring.application.name=ParkPro

# Datasource Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/park_pro
spring.datasource.username=hirwa
spring.datasource.password=Eric123!
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.properties.hibernate.default_batch_fetch_size=30

# SQL Initialization
spring.sql.init.mode=always

# Security Configuration
spring.security.user.name=admin
spring.security.user.password=admin
spring.security.user.roles=ADMIN

# JWT Configuration
app.jwt.secret=61qjfTeEMLa7M29nJhsIPPD1wqNMcbrYD+CDSrf2zdgXtYO4aw3EytyDhuI25zIl
app.jwt.expiration-ms=86400000

# Logging Configuration
logging.level.root=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.com.nanpg=DEBUG

# Management Endpoints
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
management.endpoint.metrics.enabled=true